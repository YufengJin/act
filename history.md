# act — Skill Run History

Generated by `benchmark-env-generator` at **2026-04-24T12:55:18Z**.

This file is a snapshot of what the skill did in this run — probe results, classification evidence, generated artifacts, smoke-test outcomes, final report. It is regenerated on every re-run. Commit it to track build decisions; `.gitignore` it if you treat it as throwaway build output.

## Metadata

| Field | Value |
|-------|-------|
| Target repo | `act` |
| Repo commit | `93af54d06abcc5ad641ffbd23d13a9c87c7a1578` |
| Upstream skill | `env-generator` (base docker/) |
| Sub-skill | `benchmark-env-generator` |
| Benchmark category | **IL** |
| Baselines mode | `n/a` (IL — no RL training script generated) |

## Classifier evidence

**Main criterion** (`env.step()` returns a reward / `compute_reward` exists): **partially met**. `env.step()` returns a `dm_env.TimeStep` whose `reward` field is populated by the task's `get_reward()` method (`sim_env.py` `TransferCubeTask.get_reward` and `InsertionTask.get_reward`), so a reward signal exists — but it is a stage-based sparse integer in `{0..4}`, not a continuous shaped reward suitable for RL gradient updates. The classifier therefore proceeds to the auxiliary scoring rubric.

**Auxiliary signals** (8-signal RL-readiness rubric):

| # | Signal | Result | Evidence |
|---|--------|--------|----------|
| 1 | `gym.register` / `gymnasium.register` for the env | ✗ | No `gym.register` call anywhere; envs constructed via `make_sim_env(task_name)` factory in `sim_env.py`. |
| 2 | VecEnv / SubprocVecEnv wrapper | ✗ | No vectorized-env wrapper; loops are single-env in `imitate_episodes.py` and `record_sim_episodes.py`. |
| 3 | Episode horizon advertised in env / config | ~ | `episode_len` lives in `SIM_TASK_CONFIGS` (`constants.py`), but as a per-task constant, not an env attribute. Borderline; counted as 0.5. |
| 4 | JAX-stateless / functional `step()` | ✗ | `dm_control` `step()` is stateful in-place. |
| 5 | Continuous Box `observation_space` declared | ✗ | Obs is a `dict` `OrderedDict({qpos, qvel, env_state, images})` from `dm_env.specs`, not a flat Box. |
| 6 | `algorithms/` package or `examples/training_examples/*/experiment.py` | ✗ | No such directory; only IL training (`imitate_episodes.py`) is shipped. |
| 7 | README mentions RL keywords (PPO, SAC, return, rollout, episode_return) | ✗ | README focuses entirely on imitation learning — "ACT", "behavior cloning", "demonstrations", no RL algorithm names. |
| 8 | Shaped / continuous reward function | ✗ | Stage-based sparse integer (1/2/3/4) only — no shaping terms. |

**Aux score**: **0.5 / 8** → category `IL` (threshold: RL requires main pass **and** aux ≥ 4).

## Probe summary

| Flag | Value | Evidence |
|------|-------|----------|
| `PKG_MGR` | `uv` | `uv.lock` present at repo root + `[tool.uv.workspace]` block in `pyproject.toml`. |
| `CUDA_EXT_BUILD` | `0` | No `curobo` / `flash-attn` / `mamba-ssm` / `pytorch3d` / `apex` in deps. |
| `NEEDS_VULKAN` | `0` | MuJoCo + dm_control only; no SAPIEN / ManiSkill / IsaacSim. Headless renders via OSMesa (`MUJOCO_GL=osmesa`). |
| `THIRD_PARTY_SIM` | `0` | No external `robosuite@...` / similar pin; MuJoCo + dm_control are direct deps. |
| `CONFIG_INIT` | `0` | No interactive `config_init` / `macros_private` prompt on first import. |
| `ASSET_BOOTSTRAP` | `0` | Bimanual ViperX MJCF assets ship in `assets/` directory; no download script. |
| `ACTION_DIM` (IL) | `14` | `env.action_space.shape[0]` (sim_env.py: 6 left-arm + 1 left-gripper + 6 right-arm + 1 right-gripper); baked into `tests/test_random_policy_server.py:39` metadata and verified by both clients' handshake guard. |
| `SMOKE_ENV_ID` | `sim_transfer_cube_scripted` | Smallest headless-safe env; chosen as the L1/L2/L3_IL smoke task per user selection. |

## Generated / edited files

| File | Status | Notes |
|------|--------|-------|
| `docker/Dockerfile` | pre-existing (env-generator) | uv-based, `nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu20.04`, Python 3.9 venv, MuJoCo/OSMesa apt set. Untouched in this run. |
| `docker/docker-compose.headless.yaml` | pre-existing (env-generator) | `network_mode: host`, bind-mount `..:/workspace`, HuggingFace cache mount, `MUJOCO_GL=osmesa`. Untouched. |
| `docker/docker-compose.x11.yaml` | pre-existing (env-generator) | X11 sibling profile. Untouched. |
| `docker/entrypoint.sh` | pre-existing (env-generator) | Detects nested vs flat mount, runs `uv sync --frozen --inexact` against the mounted source. Untouched. |
| `docker/README.md` | pre-existing | Day-to-day container ops doc. Untouched. |
| `scripts/run_demo.py` | **rewritten this run** | Brought to strict contract compliance: `--log_dir` default `./logs`, `<log_dir>/demo/<task>--<timestamp>/` per-run dir, `demo.log` text mirror with full `vars(args)` dump, explicit `--save_video` (default OFF), single-episode (`--n-steps`, default = `task_cfg['episode_len']`), single `demo.mp4`. |
| `scripts/run_eval.py` | **rewritten this run** | Brought to strict contract compliance: `--n-episodes` (was `--num_rollouts`), `--max-steps` (was implicit), `--log_dir` default `./logs` (was `./eval_logs`), `<log_dir>/eval/<task>--<timestamp>/` per-run dir, full `vars(args)` dump at log head (was 6 selected fields), `--save_video` default OFF (was default ON), per-episode MP4 named `episode_{ep:03d}_success={bool}.mp4`, FINAL RESULTS block matches template format. |
| `tests/test_random_policy_server.py` | previously generated | Pure WebSocket, advertises `action_dim=14`, no env construction. Contract-compliant. Untouched. |
| `install.md` | rendered previous run | Reproducibility recipe at repo root. |
| `benchmark.md` | rendered previous run | Static benchmark guide (task table + how-to). |
| `history.md` | **rendered this run** | This file. |

## Tests executed

| Tier | Command | Result |
|------|---------|--------|
| L1 (reset + 10 steps) | `python -c "from sim_env import make_sim_env; env=make_sim_env('sim_transfer_cube_scripted'); env.reset(); [env.step(np.zeros(14)) for _ in range(10)]"` | ✅ PASS |
| L2 (reward finite) | same as L1 + `assert ts.reward is not None and np.isfinite(ts.reward)` | ✅ PASS (`reward=0`, valid stage-0 reading) |
| L3_IL (WS server + run_demo + run_eval) | start `tests/test_random_policy_server.py --port 8000` (detached), socket-poll port 8000, run `scripts/run_demo.py --n-steps 30 --save_video` + `scripts/run_eval.py --n-episodes 1 --max-steps 50 --save_video` against `localhost:8000`, kill server | ✅ PASS — both clients exit 0; metadata handshake `action_dim=14` verified; `logs/demo/<task>--<ts>/{demo.log,demo.mp4}` and `logs/eval/<task>--<ts>/{eval.log,episode_000_success=False.mp4}` produced |

Captured last-5 stdout lines per tier:

**L1**:
```
reset ok, obs keys = ['qpos', 'qvel', 'env_state', 'images']
L1 OK
(dm_control teardown noise — cosmetic, ignored)
```

**L2**:
```
L2 OK: reward=0
(dm_control teardown noise — cosmetic, ignored)
```

**L3_IL — `run_demo.py` tail** (after rewrite to strict contract):
```
demo finished: 30 steps, highest_reward=0.0/4, success=False
saved video: ./logs/demo/sim_transfer_cube_scripted--20260424_125445/demo.mp4
log_file: ./logs/demo/sim_transfer_cube_scripted--20260424_125445/demo.log
```
(FAILURE is expected — random policy never reaches `max_reward=4`; the smoke is on the wiring, not on solving the task.)

**L3_IL — `run_eval.py` tail** (after rewrite to strict contract):
```
============================================================
FINAL RESULTS
============================================================
  policy:          randomPolicy
  task:            sim_transfer_cube_scripted
  episodes:        1
  mean_return:     0.000 +/- 0.000
  success_rate:    0.000
  mean_length:     50.0
  log_file:        ./logs/eval/sim_transfer_cube_scripted--20260424_125455/eval.log
============================================================
```

## Capabilities probed in the container

| Check | Expected | Result |
|-------|----------|--------|
| `nvidia-smi` | GPU visible | ✅ `NVIDIA GeForce RTX 4090` |
| `python -c "import torch; print(torch.cuda.is_available())"` | `True` | ✅ `torch 2.0.0+cu118 cuda True` |
| `python -c "import sim_env; print(sim_env.__file__)"` | resolves under `/workspace/` | ✅ `/workspace/sim_env.py` (mounted source, editable install live) |
| `python -c "from policy_websocket import WebsocketClientPolicy, WebsocketPolicyServer"` | both importable | ✅ `policy_websocket OK` |

## Final report

All four contract deliverables for an IL benchmark are now in place:

- Base `docker/` (pre-existed from `env-generator`).
- `scripts/run_demo.py` + `scripts/run_eval.py` + `tests/test_random_policy_server.py` (pre-existed; contract-audited this run).
- `install.md` + `benchmark.md` + `history.md` at repo root (rendered this run).

The four-tier smoke contract passed end-to-end: L1 (env.reset + step), L2 (finite reward), L3_IL (server + run_demo + run_eval handshake on `action_dim=14`, both clients exit 0). The image is therefore runnable end-to-end on first `docker compose up` against any user-supplied `policy_websocket`-speaking server, with the bundled random server as a default fallback.

### Known limitations

The previously-flagged deviations in `scripts/run_demo.py` (5 items: flag name, log file, args dump, save_video flag, MP4 naming) and `scripts/run_eval.py` (full `vars(args)` dump) have been **resolved this run** by rewriting both files to strict template compliance — see the *Generated / edited files* table above. The only remaining caveat:

1. The cosmetic `RuntimeError: cannot schedule new futures after shutdown` and `free(): invalid pointer` lines at process exit are a well-known `dm_control` teardown bug; they appear after the last useful line of output and do not affect any test result.

Backwards compatibility note: the historical output directories `./eval_logs/` and `./demo_log/` from earlier (pre-rewrite) runs are preserved on disk but no longer written to. New runs land under `./logs/{eval,demo}/` per the contract.

### Next actions the user can take

```bash
# Work inside the container
docker exec -it act_container bash

# Stop / restart
docker compose -f docker/docker-compose.headless.yaml down
docker compose -f docker/docker-compose.headless.yaml up -d

# Re-run the IL smoke loop (server + run_demo + run_eval)
# See install.md "Category-specific smoke run" for the full sequence
```

---

### How this file is regenerated

Re-run `benchmark-env-generator` against the repo (usually dispatched automatically by `env-generator` when the classification is `benchmark`). The skill overwrites `install.md`, `benchmark.md`, and `history.md`. To diff before overwriting, rename the old copies to `install.previous.md` / `benchmark.previous.md` / `history.previous.md` first.
